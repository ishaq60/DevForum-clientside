import {  RiseLoader } from "react-spinners"
import React from 'react';

const LoadingSppiner = () => {
    return (
        <div>
              <RiseLoader size={20} color="blue"></RiseLoader>
        </div>
    );
};

export default LoadingSppiner;